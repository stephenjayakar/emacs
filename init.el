;;; init.el --- Load Stephen's Emacs config -*- lexical-binding: t; -*-

;;; Commentary:

;; Keep Emacs startup conventions standard by loading the primary config from
;; stephen.el.

;;; Code:

(load-file (expand-file-name "stephen.el" user-emacs-directory))

(provide 'init)
;;; init.el ends here
